# Anomaly-Detection-with-Auto-Encoders
To address the highly imbalanced nature of the fraud detection dataset, an Autoencoder-based anomaly detection approach is applied.  Instead of treating the problem as a traditional supervised classification task, the model is trained primarily on normal transactions (class 0) to learn typical behavioral patterns.
